//#pragma once
//#include <cstdio>
//#include <cstdint>
//#include "windows.h"
#include "stdio.h"
#include "stdint.h"
#include "uchar.h"
#include "memory.h"
#include "stdio.h"
#include "string.h"
#include "assert.h"
#include <stdarg.h>

#define MAX(x, y) (((x) > (y)) ? (x) : (y))
#define MIN(x, y) (((x) < (y)) ? (x) : (y))
